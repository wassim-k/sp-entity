{
  "name": "sp-entity",
  "version": "1.0.5",
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "clean": "lerna run clean",
    "test": "jest --no-watchman",
    "lint": "lerna run tslint"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "devDependencies": {
    "@types/jest": "^26.0.10",
    "@types/node": "^14.6.2",
    "jest": "^26.4.2",
    "lerna": "^3.22.1",
    "ts-jest": "^26.3.0",
    "tslib": "^2.0.1",
    "tslint": "^6.1.3",
    "typescript": "^4.0.2"
  }
}
